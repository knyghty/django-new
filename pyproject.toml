[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
authors = [
    { "name" = "Tom Carrick", "email" = "tom@carrick.eu" },
]
classifiers = [
    "Development Status :: 1 - Planning",
    "Environment :: Web Environment",
    "Framework :: Django",
    "Framework :: Django :: 4.2",
    "Framework :: Django :: 5.2",
    "Framework :: Django :: 6.0",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: BSD License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = ["django"]
description = "A reference implementation for the 'new' command"
license = { "file" = "LICENSE" }
name = "django-new"
readme = "README.rst"
requires-python = ">=3.10"
version = "0.1.0"

[project.urls]
Homepage = "https://github.com/knyghty/django-new"
Repository = "https://github.com/knyghty/django-new"

[tool.coverage.run]
branch = true
source = ["."]

[tool.hatch.build.targets.sdist]
exclude = ["/tests", "requirements.txt", "runtests.py"]

[tool.hatch.build.targets.wheel]
packages = ["django_new"]

[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
select = ["DJ", "E", "F", "I", "UP", "W"]

[tool.ruff.lint.isort]
force-single-line = true
